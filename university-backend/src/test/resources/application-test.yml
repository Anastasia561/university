testcontainers:
  reuse.enable: true
  postgres:
    image: postgres:15-alpine
    username: ${TEST_USER}
    password: ${TEST_PASSWORD}
    database: ${TEST_DB}

spring:
  sql:
    init:
      mode: always
      schema-locations: file:./db/01_schema.sql

jwt:
  secret: ${JWT_SECRET}
